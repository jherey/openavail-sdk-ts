// Availsync TypeScript SDK — placeholder.
// v1 ships REST only; this SDK lands in v1.1 (per ADR D-003).
// When v1.1 starts: `pnpm generate` consumes the API's openapi.json into src/api-types.ts,
// then a thin client wrapper is layered on top.

export const SDK_VERSION = '0.0.0';
